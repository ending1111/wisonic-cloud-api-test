name: 获取某个应用下的所有权限
request:
    headers: {}
    method: GET
    params:
        appId: $appId
    url: /authorization/getActions
validate:
-   eq:
    - status_code
    - 200
variables:
-   appId: $appId
