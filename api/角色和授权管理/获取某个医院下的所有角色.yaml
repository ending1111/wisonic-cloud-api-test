name: 获取某个医院下的所有角色
request:
    headers: {}
    method: GET
    params:
        appId: $appId
        hospitalId: $hospitalId
    url: /authorization/getRoleByHospital
validate:
-   eq:
    - status_code
    - 200
variables:
-   appId: $appId
-   hospitalId: $hospitalId
