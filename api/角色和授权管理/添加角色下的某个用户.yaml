name: 添加角色下的某个用户
request:
    headers:
        Content-Type: application/json
    json:
        roleInfoId: $roleInfoId
        userId: $userId
    method: POST
    url: /authorization/addRoleUser
validate:
-   eq:
    - status_code
    - 200
variables: []
