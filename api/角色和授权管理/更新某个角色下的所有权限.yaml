name: 更新某个角色下的所有权限
request:
    headers:
        Content-Type: application/json
    json:
        actionInfoIds:
        - $actionInfoIds
        roleInfoId: $roleInfoId
    method: POST
    url: /authorization/updateRoleAction
validate:
-   eq:
    - status_code
    - 200
variables: []
