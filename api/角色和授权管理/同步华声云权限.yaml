name: 同步华声云权限
request:
    headers:
        Content-Type: application/json
    json:
        actions:
        -   actionAlias: $actionAlias
            actionName: $actionName
        serviceInfoId: $serviceInfoId
    method: POST
    url: /authorization/synchronizeAction
validate:
-   eq:
    - status_code
    - 200
variables: []
