name: 添加角色下的某个用户（通过医院id）
request:
    headers:
        Content-Type: application/json
    json:
        hospitalId: $hospitalId
        roleInfoId: $roleInfoId
        userId: $userId
    method: POST
    url: /authorization/addRoleUserByHospital
validate:
-   eq:
    - status_code
    - 200
variables: []
