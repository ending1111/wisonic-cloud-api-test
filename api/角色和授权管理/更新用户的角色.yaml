name: 更新用户的角色
request:
    headers:
        Content-Type: application/json
    json:
        newRoleInfoId: $newRoleInfoId
        roleInfoId: $roleInfoId
        userId: $userId
    method: POST
    url: /authorization/updateRoleUserByHospital
validate:
-   eq:
    - status_code
    - 200
variables: []
