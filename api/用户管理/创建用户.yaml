name: 创建用户
request:
    headers:
        Content-Type: application/json
    json:
        avatar: $avatar
        isEmployee: $isEmployee
        isRoot: $isRoot
        isTemporary: $isTemporary
        name: $name
        nickname: $nickname
        resume: $resume
        username: $username
    method: POST
    url: /user/create
validate:
-   eq:
    - status_code
    - 200
variables: []
