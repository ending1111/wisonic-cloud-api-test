name: 更新用户密码
request:
    headers:
        Content-Type: application/json
    json:
        oldPassword: $oldPassword
        password: $password
        userId: $userId
    method: POST
    url: /user/updatePassword
validate:
-   eq:
    - status_code
    - 200
variables: []
