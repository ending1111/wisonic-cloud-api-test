name: 获取用户（包括已删除用户）
request:
    headers: {}
    method: GET
    params:
        phone: $phone
        region: $region
    url: /user/getUserByPhone
validate:
-   eq:
    - status_code
    - 200
variables:
-   phone: $phone
-   region: $region
