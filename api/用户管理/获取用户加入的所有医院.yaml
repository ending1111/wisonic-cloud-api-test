name: 获取用户加入的所有医院
request:
    headers: {}
    method: GET
    params:
        principal: $principal
    url: /user/getHospital
validate:
-   eq:
    - status_code
    - 200
variables:
-   principal: $principal
