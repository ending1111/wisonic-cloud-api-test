name: 获取用户
request:
    headers: {}
    method: GET
    params:
        username: $username
    url: /user/getUserByUsername
validate:
-   eq:
    - status_code
    - 200
variables:
-   username: $username
