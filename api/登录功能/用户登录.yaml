name: 用户登录
request:
    headers:
        Content-Type: application/json
    json:
        credentials: $credentials
        hospitalId: $hospitalId
        principal: $principal
    method: POST
    url: /auth/token/principalCredentials
validate:
-   eq:
    - status_code
    - 200
variables: []
